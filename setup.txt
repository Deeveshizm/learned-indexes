install cmake if not already installed:
> brew install cmake

in terminal got to the learned-index directory:
> mkdir -p build

go to build directory:
> cd build
> cmake ..
> make
> ./leaned_index_benchmark

